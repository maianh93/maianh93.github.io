// Yêu cầu:
// - Khi click vào nút "random quote" sẽ tiến hành random 1 câu quote ra màn hình (bao gồm nội dung và tên tác giả)

// Gợi ý:
// - Mockup 1 mảng các quote, mỗi quote là 1 object bao gồm: author (tên tác giả) và content (nội dung quote)

// Tạo 1 object
let quoteObject = [
    {key, value}
];

